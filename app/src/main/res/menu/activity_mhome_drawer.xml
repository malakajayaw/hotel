<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/menu_home" />
        <item
            android:id="@+id/to_do"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Things to do" />
    </group>

    <item android:title="Contribute">
        <menu>
            <item
                android:id="@+id/add_places"
                android:icon="@drawable/ic_menu_share"
                android:title="Add Places" />
            <item
                android:id="@+id/manage_places"
                android:icon="@drawable/ic_menu_share"
                android:title="Manage Places" />
            <item
                android:id="@+id/add_hotels"
                android:icon="@drawable/ic_menu_send"
                android:title="Add Hotels" />
            <item
                android:id="@+id/manage_hotels"
                android:icon="@drawable/ic_menu_send"
                android:title="Manage Hotels" />
        </menu>
    </item>
    <item android:title="Your Profile">
        <menu>
            <item
                android:id="@+id/edit_profile"
                android:icon="@drawable/ic_menu_share"
                android:title="@string/menu_share" />
        </menu>
    </item>

</menu>
